<%= link_to 'sign_up', new_post_path %>
<% if @posts.any? %>
    <% @posts.each do |post|%>
        <div> <%=post.title%> </div>
        <div> <%=post.body%> </div>
    <%end%>
<%else%>
    <div>There are no posts at this, moment sign in to create a new post</div>
<%end%>